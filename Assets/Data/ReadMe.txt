使用说明
1、表中每一个Sheet页对应一个类，Sheet与类名必须一致，支持多个文件。
2、表中只有每一行加*才会被程序读取，默认第一行可读的数据为类的成员变量
3、run.bat为自动将xlsx格式转为txt，这里需要将两个run里面python的路径，换成你自己本地py路径。
4、数据读取使用了NGUI内部的ByteReader、BetterList，如果发生冲突，删除多余的即可。
to do
1、数据bundle加载





